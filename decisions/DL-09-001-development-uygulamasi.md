# DL-09-001: Development uygulaması — dosya yapısı düzeltmesi, test kırılganlığı giderimi, kapı doğrulama

- Tarih: 2026-07-31
- Faz: 9 — Development
- Mod: AUTOPILOT
- Karar: `docs/05-architecture.md`'deki bileşen tasarımı uygulandı: `public/physics.js` (saf fizik çekirdeği), `public/game.js` (rAF/render/HUD/input adaptörü), `public/index.html`+`style.css`, `server.js` (Express static `public/` + `/health` + güvenlik header'ları). Bir önceki koşum TASK-008 ortasında kesintiye uğramıştı (ORPHANED_RUN): TASK-001..007 diskte tamam bulundu (tüm test+impl commit çiftleri mevcut), ancak (a) `server.js` repo KÖKÜNDE bırakılmıştı (emsal projeler — coinflip/dice-game/draw-straws — hepsi `src/server.js` kullanıyor; Faz 9 mekanik kapısı "src/ mevcut ve boş değil" kontrolünü bu yüzden geçmiyordu), (b) `tests/game.test.js` ve `tests/server.test.js`'te iki kırılgan test (cooldown penceresini hesaba katmayan çift-vuruş testi; `server.listen` çağrısını `server` atamasından önce yürüten race) diskte düzeltilmiş ama commit'lenmemiş kalmıştı, (c) DL-09-001 hiç yazılmamıştı. Resume bu iş listesinden (AF-038) devam etti: TASK-001..007'yi yeniden üretmeden, yalnız TASK-008'in eksik kalan parçalarını tamamladı — `server.js` → `src/server.js`'e taşındı (`PUBLIC_DIR` artık `path.join(__dirname, '..', 'public')`), `package.json` `main`/`start` güncellendi, `tests/server.test.js` import yolu düzeltildi, bekleyen test düzeltmeleri commit'e alındı.
- Değerlendirilen alternatifler: (1) `src/` dizinine boş bir yer-tutucu dosya koyup gerçek kodu `public/`+kökte bırakmak — mekanik kapıyı "kandırır" ama emsal proje yapısından sapmayı kalıcılaştırır ve gelecekteki bir geliştiriciyi yanıltır. (2) Mekanik kapıyı (`verify-gate.mjs`) bu proje için gevşetmek — fabrika-geneli bir script'i tek proje için özel durum yaparak çürütmek kapının bütünlüğünü zedeler, kapsam dışı (fabrika değişikliği ayrı onay ister). (3) Fiziği de `src/`e taşımak — DL-05-001'in "tarayıcıya doğrudan `<script type=module>` ile giren" gerekçesini bozar (build adımı gerektirmeden hem tarayıcı hem test ortamında çalışması NFR-3'e bağlıydı); yalnız sunucu tarafı taşındı.
- Gerekçe: `src/server.js`'e taşımak hem mekanik kapıyı gerçek/dürüst şekilde geçirir hem de DL-04-002'nin referans aldığı emsal projelerle (coinflip, dice-game, snake-game, draw-straws) tutarlılığı sağlar — hiçbir onaylı doküman (`05-architecture.md`, `07-security.md`, `08-plan.md`) `server.js`'in repo KÖKÜNDE olmasını açıkça şart koşmuyordu (mermaid diyagramı ve tablo yalnız dosya adını anıyordu, yolunu değil); dolayısıyla taşıma önceki fazların kararlarıyla çelişmiyor. İki test düzeltmesi davranışı değiştirmiyor, yalnız test kurulumundaki zamanlama/varsayım hatasını gideriyor (cooldown süresi + `listen` sırası) — production kodu aynı kaldı.
- Riskler: (1) `src/server.js`'e taşıma sonrası göreli yol (`PUBLIC_DIR`) yanlış hesaplanırsa statik servis kırılır — `npm test` (25/25 yeşil, `server.test.js` dahil) ve coverage raporu bunu doğruladı. (2) İleride Faz 12 Dockerfile `COPY`/`CMD` yollarını `src/server.js`'e göre yazmalı — not düşüldü, Faz 12 kapısı bunu ayrıca denetleyecek.
- Geri alınabilirlik: Yüksek (tek dosya taşıma + iki import güncellemesi; tersine taşıma da mekanik).
- İnsan onayı: Otomatik (AUTOPILOT, kapı geçti)
- Varsayım mı?: Evet — AUTOPILOT varsayımı: emsal projelerin `src/server.js` yapısı bu proje için de doğru çözüm kabul edildi (onaylı dokümanlarda açık bir "kökte kalsın" şartı yoktu); yanlışsa Faz 10/16'da geri alınabilir.
